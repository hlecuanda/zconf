#!/usr/bin/env python3

import sys
import random
import math

def randint(a=100):
    return math.floor(random.random() * a)

if __name__ == '__main__':
    if len(sys.argv) > 1:
        print(randint(int(sys.argv[1])))
    else:
        print(randint())

    sys.exit(0)
