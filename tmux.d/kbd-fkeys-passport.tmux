
        bind-key -n F1 send-keys -l "\e[M"  #OP [M]
        bind-key -n F2 send-keys -l "\e[N"  #OQ
        bind-key -n F3 send-keys -l "\e[O"  #OR
        bind-key -n F4 send-keys -l "\e[P"  #OS
        bind-key -n F5 send-keys -l "\e[Q"  #[15~

        bind-key -n F6  send-keys -l "[R"      #[R
        bind-key -n F7  send-keys -l "[S"      #[S
        bind-key -n F8  send-keys -l "[T"      #[T
        # Bind-key -n F9  Send-keys -l "[P"      #[20~
        # Bind-key -n F10 Send-keys -l "[Q"      #[21~
        # bind-key -n F11 send-keys -l "      #[23~
        bind-key -n F12 send-keys -l "[X"

        # CSF1 [Y
        # SF2 [1;2Q
# CSF1 [Y
# SF2 [1;2Q

# vim: ft=tmux
